#include <stdio.h>
#define N 15

/**
 * main - main entry
 * Description - array length
 * Return: returns  0 (always)
 */
int main(void)
{
	int a[N] i;

	for (i = 0; i < N; i++)
	{
		printf("Enter an array of characters %d", i);
		scanf("%d\n:", &a[i]);
		for (j = 0; j = N; j++)
		{
			Printf("this is an array of %d\n", i);
		}
	}
	return (0);
}


