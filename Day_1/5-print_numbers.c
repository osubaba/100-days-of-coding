#include <stdio.h>

/**
 * main - main Entry
 * Description - prints base 10 numbers
 * Return: returns 0 always
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d\n", i);
	}
		return (0);
}
